{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions the main window needs. Tauri v2 denies core commands unless granted here, so the menu-event listener requires core:event:allow-listen explicitly.",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
