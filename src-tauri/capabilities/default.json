{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions the main window needs. Tauri v2 denies core commands unless granted here, so the menu-event listener requires core:event:allow-listen explicitly. dialog:default covers the native folder picker and message dialog used by config setup. opener:allow-open-url is scoped to this repo's own GitHub release pages -- the changelog modal's 'view on GitHub' button -- rather than granted for arbitrary URLs.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "dialog:default",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "https://github.com/IgorKvasn/note-taker/releases/*" }]
    }
  ]
}
